info:
  name: get defect visualization
  type: http
  seq: 23

http:
  method: GET
  url: http://127.0.0.1:5100/geo/api/cases/testing/stl/cavity.stl/defects
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
