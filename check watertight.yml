info:
  name: check watertight
  type: http
  seq: 4

http:
  method: GET
  url: http://127.0.0.1:5100/geo/check-watertight/testing/cavity.stl
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
