info:
  name: create projects
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:5100/projects
  body:
    type: json
    data: '{"name": "casting_test_01", "description": "Test project"}'
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
