info:
  name: get projects id
  type: http
  seq: 5

http:
  method: GET
  url: http://127.0.0.1:5100/projects/19
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
