info:
  name: delete project
  type: http
  seq: 6

http:
  method: DELETE
  url: http://127.0.0.1:5100/projects/19
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
