info:
  name: auth guard
  type: http
  seq: 3

http:
  method: GET
  url: http://127.0.0.1:5100/auth/me
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
