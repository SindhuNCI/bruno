info:
  name: Request password reset
  type: http
  seq: 4

http:
  method: POST
  url: http://127.0.0.1:5100/auth/request-password-reset
  body:
    type: json
    data: '{"email": "sindhu.murugavel@novacastindia.com"}'
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        bru.setVar("reset_token", data.reset_token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
