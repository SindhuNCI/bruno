info:
  name: delete account
  type: http
  seq: 9

http:
  method: DELETE
  url: http://127.0.0.1:5100/auth/delete-account
  body:
    type: json
    data: |-
      {
        "user_id": 2
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
