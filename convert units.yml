info:
  name: convert units
  type: http
  seq: 8

http:
  method: POST
  url: http://127.0.0.1:5100/geo/files/convert-units/testing/cavity.stl
  body:
    type: json
    data: '{"from_unit": "mm", "to_unit": "m"}'
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
