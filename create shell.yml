info:
  name: create shell
  type: http
  seq: 17

http:
  method: GET
  url: http://127.0.0.1:5100/geo/createshell_expansion/testing/5/cavity.stl/10.0
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
