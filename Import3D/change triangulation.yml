info:
  name: change triangulation
  type: http
  seq: 18

http:
  method: GET
  url: http://127.0.0.1:5100/geo/change_triangulation/testing/cavity.stl/increase/0.5
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
