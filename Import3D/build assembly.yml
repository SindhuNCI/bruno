info:
  name: build assembly
  type: http
  seq: 10

http:
  method: GET
  url: http://127.0.0.1:5100/geo/build_assembly/testing
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
