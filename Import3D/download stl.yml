info:
  name: download stl
  type: http
  seq: 15

http:
  method: GET
  url: http://127.0.0.1:5100/geo/download-stl/testing/cavity.stl
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
