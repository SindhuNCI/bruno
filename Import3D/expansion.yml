info:
  name: expansion
  type: http
  seq: 16

http:
  method: GET
  url: http://127.0.0.1:5100/geo/expansion/testing/2.5/cavity.stl
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
