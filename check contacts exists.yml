info:
  name: check contacts exists
  type: http
  seq: 11

http:
  method: GET
  url: http://127.0.0.1:5100/geo/check-contacts-exist/testing
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
